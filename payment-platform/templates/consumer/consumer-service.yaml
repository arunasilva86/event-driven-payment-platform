apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-consumer-service
spec:
  selector:
    app.kubernetes.io/component: consumer
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - protocol: TCP
      port: {{ .Values.consumer.service.port }}
      targetPort: {{ .Values.consumer.containerPort }}